<% if @_current_user.company %>

  <div class="text-center selection_menu">
    <div class="row">
    <%= link_to t('All applications'), main_all_applications_path %>
    </div>
    <div class="row">
    <%= link_to t('Pending confirmations'), main_pending_confirmations_path %>
    </div>
    <div class="row">
    <%= link_to t('Approved'), main_approved_path %>
    </div>
  </div>

<% else %>
  <h2 class="welcome">
    <%= t("Sorry")+"," %>
  </h2>
  <p class="text text-center">
    <%= t("only for companies") %><%= link_to(" "+t('student page'), main_students_path) %>
  </p>
<% end %>
